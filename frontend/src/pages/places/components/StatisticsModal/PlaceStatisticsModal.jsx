import Modal from '@/components/Modal/Modal';


export default function PlaceStatisticsModal({ title, isOpen, onClose }) {
    return (
        <Modal title={title} isOpen={isOpen} onClose={onClose}>

        </Modal>
    );
}